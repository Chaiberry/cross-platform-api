set(DOTNET_SUB_MODULES Sample)
foreach(SUBMODULE IN ITEMS ${DOTNET_SUB_MODULES})
  add_subdirectory(${SUBMODULE})
  target_link_libraries(${NATIVE_LIBRARY_NAME} PRIVATE dotnet_${SUBMODULE})
endforeach()
